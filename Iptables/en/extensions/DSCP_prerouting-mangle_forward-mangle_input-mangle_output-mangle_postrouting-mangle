This target allows to alter the value of the DSCP bits within the TOS header of the IPv4 packet. 
As this manipulates a packet, it can only be used in the mangle table.
<ul><strong>--set-dscp value</strong> - Set the DSCP field to a numerical value (can be decimal or hex)</ul>
<ul><strong>--set-dscp-class class<strong> - Set the DSCP field to a DiffServ class.</ul>
