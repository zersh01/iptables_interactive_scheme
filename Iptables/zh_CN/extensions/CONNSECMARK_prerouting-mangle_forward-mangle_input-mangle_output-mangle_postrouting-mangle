此模块将安全标签从数据包复制到连接（如果它们未标记），并从连接复制回数据包（同样仅在未标记的情况下）。通常与 SECMARK 一起使用，它在安全表中有效（为了与旧内核兼容，它在 mangle 表中也有效）。<br><br>

<ul><strong>--save</strong> — 如果数据包具有安全标签，请将其复制到连接，如果连接未标记。</ul>
<ul><strong>--restore</strong> — 如果数据包没有安全标签，而连接有，请将安全标签从连接复制到数据包。</ul>