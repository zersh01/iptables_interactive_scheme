Этот модуль копирует метки безопасности из пакетов в соединения (если они не помечены) и
из соединений обратно в пакеты (также только если они не помечены). Обычно используется вместе с SECMARK,
он действителен в таблице безопасности (для обратной совместимости со старыми ядрами он также действителен в таблице mangle).<br><br>

<ul><strong>--save</strong> — если пакет имеет маркировку безопасности, скопируйте его в соединение, если соединение не отмечено.</ul>
<ul><strong>--restore</strong> — если пакет не имеет маркировки безопасности, а соединение имеет,
скопируйте маркировку безопасности из соединения в пакет.</ul>