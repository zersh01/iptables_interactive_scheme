Эта цель позволяет изменять значение битов DSCP в заголовке TOS пакета IPv4.
Поскольку это манипулирует пакетом, она может использоваться только в таблице mangle.
<ul><strong>--set-dscp value</strong> - Установить поле DSCP в числовое значение (может быть десятичным или шестнадцатеричным)</ul>
<ul><strong>--set-dscp-class class</strong> - Установить поле DSCP в класс DiffServ.</ul>